{
  "name": "@koalafyhq/hub",
  "private": true,
  "license": "AGPL",
  "scripts": {
    "start": "next start",
    "dev": "next dev",
    "build": "next build",
    "migrate": "yarn migrate:save; yarn migrate:up; yarn prisma:generate",
    "migrate:save": "npx prisma migrate save --experimental",
    "migrate:up": "npx prisma migrate up --experimental",
    "prisma:generate": "npx prisma generate"
  },
  "dependencies": {
    "@chakra-ui/react": "^1.0.0",
    "@emotion/react": "^11.0.0",
    "@emotion/styled": "^11.0.0",
    "@prisma/client": "^2.13.1",
    "date-fns": "^2.16.1",
    "framer-motion": "^2.9.4",
    "next": "^10.0.1",
    "next-auth": "^3.1.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@prisma/cli": "^2.13.1"
  }
}
